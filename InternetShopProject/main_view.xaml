<Page x:Class="InternetShopProject.main_view"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:InternetShopProject"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="main_view">

    <Grid Background="{DynamicResource {x:Static SystemColors.GradientInactiveCaptionBrushKey}}">
        <Button Content="Przejdź" Margin="504,15,200,410" Background="{DynamicResource {x:Static SystemColors.HotTrackBrushKey}}" Click="Button_Click"/>
        <ComboBox x:Name="CatChanger" Margin="200,15,300,410"/>
        <DataGrid x:Name="ProductsTypeViever" Margin="100,50,100,30">
            <DataGridTemplateColumn>
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Click="AddToBasket_Click" Content="Dodaj do koszyka"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid>
    </Grid>
</Page>
